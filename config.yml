name: wf-ci

on:
  pull_request:
    branches:
      - develop
    types:
      - opened
      - synchronized
  push:
    branches:
      - develop

jobs:
  linting-job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome step
        run: echo "Welcome to the CI pipeline"
      - name: Checkout Repo
        uses: actions/checkout@4
      - name: Repo Information
        run:
         git branch
         echo "even name : ${{github.actior}}"
         echo "even name : ${{github.repository}}"
         echo "even name : ${{github.event}}"